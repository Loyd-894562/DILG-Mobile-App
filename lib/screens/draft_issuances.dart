import 'package:flutter/material.dart';

class DraftIssuances extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Draft Issuances'),
      ),
      body: Center(
        child: Text('Content of the Draft Issuances Page'),
      ),
    );
  }
}
